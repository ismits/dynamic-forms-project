import { Button } from "@mui/material"

function FormContainerButton() {
  return (
    <div>
      <Button onClick={() => {}} variant="outlined" size="small">
        Edit
      </Button>
      <Button onClick={() => {}} variant="outlined" size="small">
        Save
      </Button>
    </div>
  )
}

export default FormContainerButton